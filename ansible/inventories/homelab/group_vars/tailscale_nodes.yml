---
# Tailscale 관리 평면 설정 (infra-bastion).

tailscale_enable_ssh: true
tailscale_accept_routes: true

# infra-bastion 을 subnet router 로 동작시키려면 Proxmox/VM LAN CIDR 을 지정.
# 예) "10.0.0.0/24". 빈 문자열이면 advertise-routes 옵션을 추가하지 않습니다.
tailscale_advertise_routes: ""

tailscale_advertise_exit_node: false
