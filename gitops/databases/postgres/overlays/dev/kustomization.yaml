apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: postgres-dev
nameSuffix: -dev

resources:
  - ../../base
  - api-common  #(App)CommonPlant

patches:
  - path: patch-cluster.yaml

generators:
  - postgres-dev-secret-generator.yaml