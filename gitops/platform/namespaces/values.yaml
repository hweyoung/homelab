namespaces:
  # ==================
  # Platform Namespace
  # ==================
  - name: infisical
    environment: prod
    gatewayAccess: public

  - name: external-secrets
    environment: prod
    gatewayAccess: private

  # ==================
  # Homelab Workload Namespace
  # ==================
  - name: api-nginx-dev
    environment: dev
    gatewayAccess: public

  - name: api-nginx-prod
    environment: prod
    gatewayAccess: public
