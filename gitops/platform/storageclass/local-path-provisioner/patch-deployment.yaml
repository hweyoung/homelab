apiVersion: apps/v1
kind: Deployment
metadata:
  name: local-path-provisioner
  namespace: local-path-storage
spec:
  template:
    spec:
      tolerations:
        - key: homelab.okbear.dev/environment
          operator: Exists
          effect: NoSchedule