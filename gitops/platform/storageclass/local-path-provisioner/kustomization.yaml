apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Pin the upstream release so GitOps renders are reproducible.
resources:
  - github.com/rancher/local-path-provisioner/deploy?ref=v0.0.36

patches:
  - path: patch-storageclass.yaml
