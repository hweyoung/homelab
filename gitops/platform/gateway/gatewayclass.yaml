# 공용 GatewayClass — Traefik 이 Gateway API Controller 로 처리한다.
# Traefik Helm chart 는 gatewayClass.enabled: false 로 두고 여기서 명시적으로 만든다.
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: traefik
spec:
  controllerName: traefik.io/gateway-controller
