apiVersion: v1
kind: Service
metadata:
  name: garage
spec:
  type: ClusterIP
  ports:
    - name: s3
      port: 3900
      targetPort: s3
    - name: web
      port: 3902
      targetPort: web
  selector:
    app.kubernetes.io/name: garage
    app.kubernetes.io/instance: platform-garage
