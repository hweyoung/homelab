apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels:
    app.kubernetes.io/name: nginx
spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: nginx

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http