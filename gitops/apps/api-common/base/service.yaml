apiVersion: v1
kind: Service
metadata:
  name: api-common
  labels:
    app.kubernetes.io/name: api-common
spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: api-common

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http